import { PlusSizeDirective } from './plus-size.directive';

describe('PlusSizeDirective', () => {
  it('should create an instance', () => {
    const directive = new PlusSizeDirective();
    expect(directive).toBeTruthy();
  });
});
